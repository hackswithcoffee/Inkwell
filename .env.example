OLLAMA_HOST=http://localhost:11434

# Optional — uncomment to also copy the recap and master files to a local
# Google Drive (or similar sync client) folder after each successful run.
# Not required; the pipeline runs fine without it.
# DRIVE_SYNC_DIR=/Users/you/Google Drive/My Drive/Inkwell
